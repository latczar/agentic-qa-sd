[tool.pytest.ini_options]
pythonpath = [".", ".."]
markers = [
    "ollama: needs a real Ollama with the embedding model pulled; skipped automatically when Ollama is unreachable, and excluded in CI",
]
